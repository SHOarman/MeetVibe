import 'package:flutter/material.dart';

class Eventdetels extends StatelessWidget {
  const Eventdetels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
